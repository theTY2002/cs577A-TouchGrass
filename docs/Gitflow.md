# Gitflow — Contribution & Workflow

For full details on pushing code, branching, and pull requests, see:

**[GitHub Workflow Guide](./GITHUB_WORKFLOW.md)**

---

## Quick Reference

- Create a branch: `git checkout -b my-feature-branch`
- Stage & commit: `git add .` → `git commit -m "Your message"`
- Push: `git push -u origin my-feature-branch`
- Open a pull request on GitHub when ready to merge
